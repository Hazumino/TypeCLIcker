#include"../include/type_function_sentence.h"

#define TOTSENTENCE 5 

// Keyboard practice functions

void kbPractice_sentence()
{

  int random, count, row, col, y, x, groupCount;
  int maxX = 10+x;
  int charIndex = 0;
  int yPos = 0;
  int xPos = 0;
  int lineNum = 0;

  float wordCount = 0;
  float errors = 1;
  float charcount = 1;
  float accuracy = 0;

  bool wordFinished = false;

  char **word = getList(1, 2, 0) ;
  printf("%s", word[1]);
}
